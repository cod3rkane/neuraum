import React from 'react';

import HouseContainer from '../../containers/House';

import './Home.scss';

export const Home = () => {
  return (
    <div id="home">
        <header>header</header>
        <HouseContainer />
      </div>
  );
};

export default Home;
